""" 
Alexis Kaldany Final Exam
"""
import torch 
from torch.utils.data import Dataset, DataLoader



""" 
Reorganize the data into a format that can be used by the model.
input = 'input'
target = 'target'
"""
dataframe = ""
input_column = ""
target_column = ""

train_dataframe = 
train_dataloader = DataLoader(train_dataframe, batch_size=32, shuffle=True)

""" 
MLP
"""


"""
RNN
"""

""" 
Transformer
"""
model1_name = 'howey/electra-small-mnli'
